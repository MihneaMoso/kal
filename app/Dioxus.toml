[application]
# Kal — local-first calendar
default_platform = "desktop"

[app]
name = "kal"
title = "Kal"

[web.app]
title = "Kal"
# The web build is deployed at /kal/app/ on GitHub Pages (project site under
# the `/kal/` path prefix, with the app in its own subdirectory).
base_path = "kal/app"

[desktop]
# Window defaults for the desktop shell

[android.app]
min_sdk_version = 30
target_sdk_version = 34
compile_sdk_version = 34

[bundle]
identifier = "com.kal.calendar"
publisher = "kal"
