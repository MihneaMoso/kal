[package]
name = "chrono-notify"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "Chrono chrono-notify crate (stub — implemented in later phases)."

[dependencies]
