[package]
name = "chrono-sync"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "Chrono chrono-sync crate (stub — implemented in later phases)."

[dependencies]
