[package]
name = "kal-core"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "Pure Rust domain logic for Kal calendar: models, recurrence, view-models. No UI deps."

[dependencies]
chrono = { workspace = true }
serde = { workspace = true }
ulid = { workspace = true }
thiserror = { workspace = true }
rrule = "0.14"

[dev-dependencies]
serde_json = { workspace = true }
